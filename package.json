{
  "name": "rabe-js",
  "version": "0.1.0",
  "description": "javascript rabe wrapper",
  "main": "rabe.node",
  "scripts": {
    "build": "cargo-cp-artifact -nc rabe.node -- cargo build --message-format=json-render-diagnostics",
    "build-debug": "npm run build --",
    "build-release": "npm run build -- --release",
    "install": "npm run build-release",
    "test": "cargo test"
  },
  "author": "PPL",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
